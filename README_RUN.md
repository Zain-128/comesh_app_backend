# Backend run commands

**Development (with watch):**
```bash
cd comesh-backend-
npm run start:dev
```

**Production:**
```bash
npm run build
npm run start:prod
```

**PM2:**
```bash
npm run start:pm2
```

Server default: `http://localhost:3000` (ya jo bhi `PORT` .env mein ho).
